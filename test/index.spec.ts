describe('index', () => {
  describe('myPackage', () => {
    it('should return a string containing the message', () => {
      const result = 'a';
      const test = 'a';

      expect(result).toMatch(test);
    });
  });
});
